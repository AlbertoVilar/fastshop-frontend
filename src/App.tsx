import { FC } from 'react';

const App: FC = () => (
  <main style={{ margin: '2rem auto', maxWidth: '640px', fontFamily: 'system-ui, sans-serif' }}>
    <h1 style={{ fontSize: '2rem', marginBottom: '0.5rem' }}>Fastshop Frontend</h1>
    <p style={{ color: '#555', lineHeight: 1.6 }}>
      Projeto inicial limpo. Adicione seus componentes em <code>src</code>.
    </p>
  </main>
);

export default App;
